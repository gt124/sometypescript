const {TheUnitTestAgain} = require("./theUnitTestAgain");

test('adds 1 + 2 to equal 3', () => {
    let sut = new TheUnitTestAgain();
    expect(sut.CalcArea(1, 3)).toBe(3);

    joe = {asut: "some",
        redos: "other"
    }
    console.log(joe.redos)
});